# Environment variables for Time Series Anomaly Detection

# Project root (auto-set)
PROJECT_ROOT=${PWD}

# Logging
LOG_LEVEL=INFO

# MLflow (optional)
MLFLOW_TRACKING_URI=http://localhost:5000

# Device (Apple Silicon)
DEVICE=cpu  # or mps if torch MPS available
